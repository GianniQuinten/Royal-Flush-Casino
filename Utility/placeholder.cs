
//InputHelper:
//This class can contain methods for handling user input, such as reading input from the console, validating user input, converting input to appropriate data types, etc.
//Example methods:
//ReadInteger(string prompt): Reads an integer value from the console after displaying the provided prompt.
//ReadDouble(string prompt): Reads a double value from the console after displaying the provided prompt.
//ReadString(string prompt): Reads a string from the console after displaying the provided prompt.

//OutputHelper:
//This class can contain methods for displaying output to the console, formatting output, or logging messages.
//Example methods:
//DisplayMessage(string message): Displays the provided message on the console.
//FormatCurrency(decimal amount): Formats a decimal value as currency (e.g., "$1,234.56").
//LogError(Exception ex): Logs an error message along with the exception details.

//ValidationHelper:
//This class can contain methods for data validation, such as checking if a string represents a valid email address, validating numeric ranges, etc.
//Example methods:
//IsValidEmail(string email): Checks if the given string is a valid email address.
//IsInRange(int value, int min, int max): Checks if an integer value falls within a specified range.

//DateHelper:
//This class can contain methods for working with dates and times, such as formatting dates, calculating durations, or parsing date strings.
//Example methods:
//FormatDate(DateTime date): Formats a DateTime object as a string in a specified format.
//CalculateAge(DateTime birthDate): Calculates the age based on the provided birth date.

//FileHelper:
//This class can contain methods for file handling operations, such as reading/writing files, checking file existence, or manipulating file paths.
//Example methods:
//ReadFile(string filePath): Reads the contents of a file and returns them as a string.
//WriteFile(string filePath, string content): Writes the provided content to a file at the specified path.